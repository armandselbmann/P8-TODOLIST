{% extends 'base.html.twig' %}

{% block body %}
    <div class="row m-3">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right mt-3">Ajouter</button>
        {{ form_end(form) }}

        <a href="{{ path('task_list') }}" class="btn btn-primary m-3">Retour à la liste des tâches</a>
    </div>
{% endblock %}
